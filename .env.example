# Dedicated port for mgaminggala.fun ONLY — must match nginx proxy_pass for this domain.
# Find existing port: grep -r "mgaminggala.fun" /etc/nginx/sites-enabled/
# Do NOT use a port assigned to another project.
PORT=3010

CIVMTN_PID=3
# CIVMTN_API_BASE=http://168.144.122.72/prod
